import 'package:flutter/material.dart';

class AddDuelScreen extends StatelessWidget {
  const AddDuelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("AddDuelScreen"));
  }
}
